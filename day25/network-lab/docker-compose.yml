version: "3.9"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    networks:
      - frontend
      - backend

  redis:
    image: redis:alpine
    networks:
      - backend

networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge

